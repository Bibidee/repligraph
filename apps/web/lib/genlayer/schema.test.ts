import {describe,expect,it} from 'vitest';import {parseEdge,parseNeighbor,parseRelationClaim,parseStudy,parseStudies} from './schema';
const study={study_id:1,registrant:'0xabc',title:'Title',question_text:'Q',method_text:'M',conclusion_text:'C',manifest_url:'https://x',manifest_digest:'a'.repeat(64),publication_ref:'doi',version:1,created_at:0};
const claim={claim_id:1,source_id:1,target_id:2,source_version:1,target_version:1,claimant:'0xabc',claimed_relation:'EXTENSION',evidence_url:'https://x',evidence_digest:'b'.repeat(64),status:'RELATION_CLAIMED',final_relation:'',outcome_class:'',rationale:'',reviewed_at:0};
const edge={edge_id:1,claim_id:2,source_id:1,target_id:2,source_version:1,target_version:1,relation_code:'EXTENSION',rationale:'r',accepted_at:0};
describe('runtime schemas',()=>{it('parses a complete study',()=>expect(parseStudy(study).title).toBe('Title'));it.each(['study_id','registrant','manifest_url','version','created_at'])('rejects malformed study %s',(key)=>expect(()=>parseStudy({...study,[key]:null})).toThrow(/Malformed study/));it('parses arrays',()=>expect(parseStudies([study])).toHaveLength(1));it('rejects non-array study lists',()=>expect(()=>parseStudies(study)).toThrow(/list/));it('parses a complete claim',()=>expect(parseRelationClaim(claim).status).toBe('RELATION_CLAIMED'));it.each(['claim_id','source_version','outcome_class','reviewed_at'])('rejects malformed claim %s',(key)=>expect(()=>parseRelationClaim({...claim,[key]:null})).toThrow(/Malformed relation/));it('parses a complete edge',()=>expect(parseEdge(edge).claim_id).toBe(2));it.each(['edge_id','claim_id','accepted_at'])('rejects malformed edge %s',(key)=>expect(()=>parseEdge({...edge,[key]:null})).toThrow(/Malformed edge/));it('normalizes a numeric distance string',()=>expect(parseNeighbor({study_id:2,field_kind:'QUESTION',version:1,distance:'0.42',title:'N'}).distance).toBe(.42));it('rejects nonfinite distance',()=>expect(()=>parseNeighbor({study_id:2,field_kind:'QUESTION',version:1,distance:'NaN',title:'N'})).toThrow(/distance/));it('rejects negative distance',()=>expect(()=>parseNeighbor({study_id:2,field_kind:'QUESTION',version:1,distance:-1,title:'N'})).toThrow(/distance/))});
