import {describe,expect,it} from 'vitest';import {newestClaim,newestStudy} from './workflow';
const study=(id:number,registrant='0xabc')=>({study_id:id,registrant,title:'T',question_text:'Q',method_text:'M',conclusion_text:'C',manifest_url:'https://x',manifest_digest:'a'.repeat(64),publication_ref:'p',version:1,created_at:0});const claim=(id:number,claimant='0xabc',status='RELATION_CLAIMED')=>({claim_id:id,source_id:1,target_id:2,source_version:1,target_version:1,claimant,claimed_relation:'EXTENSION',evidence_url:'https://x',evidence_digest:'b'.repeat(64),status,final_relation:'',outcome_class:'',rationale:'',reviewed_at:0});
describe('authoritative workflow resolution',()=>{it('finds the newly registered study',()=>expect(newestStudy([study(1)],[study(1),study(2)],'0xABC')?.study_id).toBe(2));it('does not claim another registrants study',()=>expect(newestStudy([], [study(1,'0xdef')],'0xabc')).toBeNull());it('finds the newly created claim',()=>expect(newestClaim([claim(1)],[claim(1),claim(2)],'0xABC')?.claim_id).toBe(2));it('does not guess a missing claim',()=>expect(newestClaim([claim(1)],[claim(1)],'0xabc')).toBeNull());it.each(['REVIEW_RETRYABLE','EVIDENCE_INVALID','INSUFFICIENT','EDGE_ACCEPTED'])('preserves lifecycle state %s',(status)=>expect(claim(1,'0xabc',status).status).toBe(status))});
