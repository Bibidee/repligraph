import {describe,expect,it} from 'vitest';import {ExecutionResult} from 'genlayer-js/types';import {inspectExecution} from './execution';describe('GenVM execution',()=>{it('accepts finalized execution return',()=>expect(inspectExecution({txExecutionResultName:ExecutionResult.FINISHED_WITH_RETURN}).ok).toBe(true));it('rejects rollback',()=>expect(inspectExecution({txExecutionResultName:ExecutionResult.FINISHED_WITH_ERROR})).toMatchObject({ok:false,kind:'GENVM_ROLLBACK'}));it('does not treat unknown execution as success',()=>expect(inspectExecution({})).toMatchObject({ok:false,kind:'CONSENSUS_PENDING'}))});
