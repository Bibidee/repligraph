import {describe,expect,it,vi} from 'vitest';import {validateSigningContext} from './contract';
const provider=(chain='0xf22f',accounts:any=['0xAbC'])=>({request:vi.fn(async({method}:{method:string})=>method==='eth_chainId'?chain:accounts)});
describe('signing preflight',()=>{it('accepts the expected account on StudioNet',async()=>expect(await validateSigningContext(provider(),'0xabc')).toBe('0xabc'));it('rejects a missing provider',async()=>await expect(validateSigningContext(null,'0xabc')).rejects.toThrow(/No injected/));it('rejects the wrong chain',async()=>await expect(validateSigningContext(provider('0x1'),'0xabc')).rejects.toThrow(/Wrong network/));it('rejects no accounts',async()=>await expect(validateSigningContext(provider('0xf22f',[]),'0xabc')).rejects.toThrow(/no currently authorized/));it('rejects malformed accounts',async()=>await expect(validateSigningContext(provider('0xf22f',null),'0xabc')).rejects.toThrow(/no currently authorized/));it('rejects account mismatch',async()=>await expect(validateSigningContext(provider('0xf22f',['0xdef']),'0xabc')).rejects.toThrow(/account changed/));it('checks chain before accounts',async()=>{const p=provider();await validateSigningContext(p,'0xabc');expect(p.request.mock.calls.map(c=>c[0].method)).toEqual(['eth_chainId','eth_accounts'])})});
